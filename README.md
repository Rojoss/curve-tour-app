# CFP Tournament app

## Setup

- Install the latest stable version of Node.js if you haven't already.
- Install pnpm if you haven't already: `npm install -g pnpm`
- Then run `pnpm install`

## Development

- Run `pnpm dev` to start the development server.

## Build

- Run `pnpm build` to build the app for production.
